# Production Frontend Runner (Prebuilt Artifact)
FROM nginx:alpine

# Copy prebuilt frontend static assets
COPY frontend/dist /usr/share/nginx/html

# Copy custom nginx configuration (placed in root of deployment artifact)
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
